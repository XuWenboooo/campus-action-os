# Definition of done

A foundation change is complete only when its runtime behavior is exercised, failure paths are retained, synthetic data origin is explicit, schema and audit semantics are preserved, and the relevant Windows-compatible command has passed. Passing tests do not by themselves prove the frozen 800-item benchmark, WeChat Developer Tools behavior, real-provider quality or a production deployment.
